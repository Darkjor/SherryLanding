# Sherry Favicon Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the default star favicon with a custom burgundy background and gold "S" serif letter favicon.

**Architecture:** Update the existing `public/favicon.svg` with the approved SVG code.

**Tech Stack:** SVG, Astro

---

### Task 1: Update Favicon SVG File

**Files:**
- Modify: [favicon.svg](file:///c:/Users/Perez/OneDrive/Escritorio/Projects/SherryLanding/public/favicon.svg)

- [ ] **Step 1: Replace favicon.svg content**

Write the following code to `public/favicon.svg`, overwriting its contents:

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" fill="#5C1A2E"/>
  <text x="16" y="23" text-anchor="middle" font-family="'Cormorant Garamond', 'Playfair Display', Georgia, serif" font-weight="bold" font-size="20" fill="#B8963E">S</text>
</svg>
```

- [ ] **Step 2: Commit the changes**

Run:
```powershell
git add public/favicon.svg
git commit -m "feat: update favicon to approved burgundy and gold S design"
```
