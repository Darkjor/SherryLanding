---
interface Props {
  class?: string;
  width?: string;
}
const { class: className = '', width = 'w-14' } = Astro.props;
---

<div class={`${width} h-px bg-gold my-6 ${className}`} role="presentation" aria-hidden="true"></div>
