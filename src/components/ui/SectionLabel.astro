---
interface Props {
  class?: string;
  color?: string;
}
const { class: className = '', color = 'text-gris' } = Astro.props;
---

<p class={`font-sans text-xs tracking-widest uppercase mb-4 ${color} ${className}`}>
  <slot />
</p>
